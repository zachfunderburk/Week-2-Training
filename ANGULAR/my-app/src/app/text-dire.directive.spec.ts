import { TextDireDirective } from './text-dire.directive';

describe('TextDireDirective', () => {
  it('should create an instance', () => {
    const directive = new TextDireDirective();
    expect(directive).toBeTruthy();
  });
});
